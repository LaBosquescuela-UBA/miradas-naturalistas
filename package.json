{
  "name": "miradas-naturalistas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy --only hosting:miradas-naturalistas --project labosquescuela-uba"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0",
    "playwright": "^1.59.1",
    "sharp": "^0.34.5",
    "vite": "^5.4.0"
  }
}
